import ModelFollowing from './CreatingObject.jsx';

const arrModelFollowing = [
    new ModelFollowing('Channels', 1, true),
    new ModelFollowing('Live', 2, false),
    new ModelFollowing('Videos', 3, false)
];

export {arrModelFollowing};

